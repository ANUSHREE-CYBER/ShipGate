# Steps 1-3 (rule engine, synthetic generator, latent outcome) are pure stdlib Python.
# xgboost / shap / fastapi get added at the step that first needs them.
pytest==8.3.4

# Step 4 (evaluation): average_precision_score for PR-AUC. Nothing else is used -
# the confusion matrices and cost table are computed directly, so the numbers stay
# readable and auditable rather than hidden behind a library call.
scikit-learn==1.9.0
